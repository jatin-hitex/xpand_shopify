<style>
  {% if request.path contains '/faqs' %}
    .rte {
      font-family: 'neuzeit_grotesk_light';
      margin: 0 0 15px;
      font-style: normal;
      line-height: 1.6em;
      font-size: 15px;
      color: #4a4a4a;
    }
    .page-narrow .content a.button {
      background: #008ffd;
      color: #fff;
      border: 1px solid #008ffd;
      font-weight: 700;
      letter-spacing: 1.2px;
      text-transform: uppercase;
      padding: 0 20px;
      font-size: 14px;
      height: 40px;
      display: inline-block;
      line-height: 40px;
      border-radius: 5px;
    }
  {% endif %}
</style>
<section class="section">
  <div class="
                              container
                              content
                              page
                            ">
    <div class="
                                        three-fourths
                                        columns
                                        medium-down--one-whole
                                        offset-by-two
                                        is-hidden-offset-mobile-only
                                      ">
      <h1>{{ page.title }}</h1>
      <div class="feature-divider"></div>

      <div class="rte">
        {{ page.content }}
      </div>
    </div>
  </div>
</section>